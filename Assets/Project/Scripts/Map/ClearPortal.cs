using UnityEngine;

public class ClearPortal : MonoBehaviour
{
    /*
    // 중복 충돌 방지
    private bool _isActivated = false;

    private async void OnTriggerEnter2D(Collider2D other)
    {
        if (other.CompareTag("Player") && !_isActivated)
        {
            _isActivated = true;
            Debug.Log("클리어 포탈 진입.. 다음 스테이지로 이동");

            
            
            await SceneLoader.LoadScene(nextStageId);
        }
    }
    */
}
